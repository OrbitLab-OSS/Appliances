[Unit]
Description=ETCD Bootstrap
Wants=network.target
After=network.target
ConditionPathExists=!/etc/default/etcd

[Service]
Type=oneshot
ExecStart=/usr/bin/etcd-init
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target